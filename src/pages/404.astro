---
import Layout from "../layouts/Layout.astro";
import { getEntryBySlug } from "astro:content";

const about = await getEntryBySlug("e404", "index");
const { title, subtitle, button_text } = about.data;
---

<Layout title="404" ogTitle="404" description="" blackLogo>
    <div class="container">
        <div class="text">
            <h2 class="text-large-title">{title}</h2>
            <div class="text-title-1">{subtitle}</div>
        </div>
        <a href="/">
            <button class="main-button">{button_text}</button>
        </a>
    </div>
</Layout>

<style lang="scss">
  @import "@styles/global.scss";
  
  .container {
    display: flex;
    height: 100dvh;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 72px;
  }
  
  .text {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    gap: 24px;
  }
</style>

